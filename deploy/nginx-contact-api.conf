# ============================================================
# keepbreath.ing — contact API (nginx reverse-proxy snippet)
# Author: Mark Thompson
# ============================================================
# Reference copy of the `location /api/contact` block that goes
# into the site's nginx server config on the droplet. Version-
# controlled here alongside the systemd unit.
#
# Job: rate-limit the endpoint, then pass matching requests to the
# gunicorn service on loopback. Scaffold — directives filled in
# together at the deploy step.

# --- Rate limit zone (goes in the http {} context, not here) ---
# TODO: define a limit_req_zone keyed on client IP (a shared memory
#       zone + a modest rate, e.g. a few requests/minute).

# --- The endpoint (goes inside the existing server {} block) ---
# location /api/contact {
#     TODO: apply limit_req (with a small burst), cap the body size
#           (client_max_body_size), and proxy_pass to the gunicorn
#           service on 127.0.0.1:<port> — forwarding the real client
#           IP so rate limiting and logs are accurate.
# }
