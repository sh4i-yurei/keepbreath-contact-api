# keepbreath.ing — contact API dependencies
# smtplib and email are in the Python standard library; these are the third-party
# pieces (web framework, its server, email validation, bot defense, logging).
# Pinned to exact versions for reproducible builds (resolved 2026-08-14).
Flask==3.1.3
gunicorn==26.0.0
email-validator==2.3.0
structlog==26.1.0

# altcha is deliberately NOT exact-pinned yet. The app's create_challenge /
# verify_solution calls don't match a single released API (create_challenge args
# match 2.x; verify_solution's check_expires + .verified do not), so the exact
# version gets chosen when the ALTCHA integration is reworked and validated
# against the live widget. Kept >=1.0.0 as the CVE-2025-68113 security floor.
altcha>=1.0.0
